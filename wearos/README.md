# Wear OS companion

This module is included from the repository root Gradle project.

```bash
# from repo root
./gradlew :wearos:assembleDebug
```

It does not mine. Start/stop and stats go through the Wearable Data Layer to the phone app (`com.iml1s.xmrigminer`).
