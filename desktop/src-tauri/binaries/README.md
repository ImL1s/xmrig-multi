# Place `xmrig` / `xmrig.exe` here after `desktop/scripts/build-xmrig.sh`.
#
# Binaries are gitignored. Tauri looks for `binaries/xmrig` (or `.exe` on Windows)
# next to the app and in this folder during development.
