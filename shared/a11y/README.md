# Accessibility acceptance helpers (#58)

Viewport + contrast + focus heuristics for CI. Native TalkBack/VoiceOver remain **manual / unverified** until recorded.

```js
import { VIEWPORTS, contrastRatio, checkUiSnapshot } from './js/checks.js';
```
